import React from 'react';

const SearchInfo = ({ cardData }) => {
  const renderSearchInfo = () => {
    if (cardData?.length === 0) {
      return '';
    }

    if (cardData?.length > 0) {
    }
  };

  return (
    <div>
      <Container></Container>
    </div>
  );
};

export default SearchInfo;
