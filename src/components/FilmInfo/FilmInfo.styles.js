import { styled } from '@mui/material';

export const FilmInfoBlock = styled('div')({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  paddingBottom: 30,
});

export const FilmInfoFlexBlock = styled('div')({
  display: 'flex',
});

export const Rating = styled('div')({
  marginLeft: 20,
  border: '1px solid #eee',
  borderRadius: '50%',
  color: '#eee',
  padding: '10px',
});

export const TypographyBlock = styled('div')({});
