import { styled } from '@mui/material';

export const CardBlock = styled('div')({
  padding: '30px 0',
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  flexWrap: 'wrap',
});
